var Fluxxor = require("fluxxor");
var actions = require("actions");
var filter  = require('lodash.filter');
var where   = require('lodash.where');


var DisabilityStore = Fluxxor.createStore({
  initialize: function(options) {
    this.data = options.data
    // this.selected = []

    // this.bindActions(
    //   actions.constants.Disability.SELECT, this.handleSelectDisability

    //   // actions.constants.EMPLOYMENT.LOAD, this.handleLoad,
    //   // actions.constants.EMPLOYMENT.LOAD_SUCCESS, this.handleLoadSuccess,
    //   // actions.constants.EMPLOYMENT.LOAD_FAIL, this.handleLoadFail
    // );
  },


  query: function (match) {
    //TODO more query methods
    if (!match) return  this.data
    return where(this.data, match)
  }

  //NOTE: methods used if routeStore waitsFor for DisabilityStore to update

  // getState: function () {
  //   return {
  //     selected: this.selected
  //   }
  // },

  // handleSelectDisability: function (payload) {
  //   this.selected = where(this.data, { id: payload.Disability })
  // }


});

module.exports = DisabilityStore;
