//main
var React       = require('react')

//helpers

var _ 					= require('lodash')

//logging
var log       	= require('debug')('components:indicator:table-row')

module.exports = React.createClass({

	propTypes : {
		flux: React.PropTypes.object,
    breakdownMap: React.PropTypes.object,
    datum: React.PropTypes.object,
    breakdownOrder: React.PropTypes.array
    // params: React.PropTypes.object,
		// tableHeaderGroups: React.PropTypes.array
	},


  render: function () {
		log('props', this.props)
		var flux = this.props.flux



		return (
			<tr className='snapshot'>
        {_.map(this.props.breakdownOrder, function (breakdown) {
          return <td>{}
        }).concat(
        <td>{datum.value}</td>
      )}
			</tr>

  )


  },

	handleHeaderColumnClick: function (id) {
		return function () {
			//TODO
		}
	}


})
