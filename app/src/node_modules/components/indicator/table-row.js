//main
var React       = require('react')

//helpers

var _ 					= require('lodash')

//logging
var log       	= require('debug')('components:indicator:table-row')

module.exports = React.createClass({

	propTypes : {
    shortToTitle: React.PropTypes.object,
    datum: React.PropTypes.object,
    breakdownIds: React.PropTypes.array, //ordered
	},

  render: function () {
		log('props', this.props)
		var flux = this.props.flux
    var d = this.props.datum

		return (
			<tr className='snapshot'>
        {
          _.map(this.props.breakdownIds, function (breakdownId) { //ordered
            return <td>{this.props.shortToTitle[d[breakdownId]]}</td> //outputs datum group in the correct order
          }).concat(
            <td>{datum.value}</td>
          )
        }
			</tr>

  )


  },

	handleHeaderColumnClick: function (id) {
		return function () {
			//TODO
		}
	}


})
