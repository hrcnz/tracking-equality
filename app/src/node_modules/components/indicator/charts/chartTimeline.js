//main
var React       = require('react')

//helpers
var _ 					= require('lodash')

//logging
var log       	= require('debug')('components:indicator:timeline')



module.exports = React.createClass({

	propTypes : {
		flux        : React.PropTypes.object,
    datasets    : React.PropTypes.array, // datasets/timesteps    
		dimensions  : React.PropTypes.array, // dimensions
		groups      : React.PropTypes.array, // groups for each dimension
    data        : React.PropTypes.array, // filtered data by datasets,dimensions&groups (including all 'alls'
    ranges      : React.PropTypes.array  // the top/bottom values and group labels for all datasets  
	},


  render: function () {
		log('props', this.props)
        
    
		
  }


})
