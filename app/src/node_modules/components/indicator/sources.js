//main
var React       = require('react')

// components
var Row         = require('react-bootstrap/lib/Row')

//helpers
var _           = require('lodash')

//logging
var log         = require('debug')('components:indicator:sources')



module.exports = React.createClass({

  propTypes : {
    datasets    : React.PropTypes.array.isRequired, // datasets/timesteps
  },


  render: function () {
    log('props', this.props)

    return (
       <div className='sources'>
       <p><strong>Sources</strong></p>
        {
        _.map(this.props.datasets, function (dataset) {
          return (
            <p>
                <span className='dataset-title'><strong>{dataset.title}</strong></span>
                <span className='dataset-publisher'>{dataset.publisher}</span>
                <span className='dataset-date'>{dataset.publicationdate}</span>
            </p>
          )
        })
      }
      </div>
    )


  }


})
