//main
var React       = require('react')

// components
var Row         = require('react-bootstrap/lib/Row')

//helpers
var _ 					= require('lodash')

//logging
var log       	= require('debug')('components:indicator:sources')



module.exports = React.createClass({

	propTypes : {
    datasets    : React.PropTypes.array.isRequired, // datasets/timesteps
	},


  render: function () {
		log('props', this.props)
    var title = this.props.datasets.length > 1 ? "Sources" : "Source"


    return (
      <Row style={{paddingLeft: 45}}>
      <Row>
        <h3>{title}</h3>
      </Row>
      {
        _.map(this.props.datasets, function (dataset) {
          return (
            <Row>
              <p>
                {
                  [
                    <strong>{dataset.title + ','}</strong>, <br></br>,
                    'published by ' + dataset.publisher + ' on', <br></br>,
                    dataset.publicationdate
                  ]
                }
              </p>
            </Row>
          )
        })
      }
      </Row>
    )


  }


})
