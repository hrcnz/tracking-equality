var React         = require("react")
var Router        = require("react-router")
var About         = require('components/about')
var Main          = require('components/main')
var Intro         = require('components/intro')
var Issue         = require('components/issue')
var Indicator     = require('components/indicator')

var Route         = Router.Route
var DefaultRoute  = Router.DefaultRoute

require('string.prototype.startswith')

var routes = (
  <Route path={"/"} handler={Main} name="home" >
    <DefaultRoute handler={Intro} name="intro" />
    <Route path={"/about"} handler={About} name="about" />
    <Route path={"/issue/:issueSlug"} handler={Issue} name="issue" />
    <Route path={"/indicator/:indicatorSlug"} handler={Indicator} name="indicator" />
  </Route>
);

module.exports = routes;
