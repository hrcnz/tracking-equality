module.exports = function (sortDesc, value, refValue) {          
    if (typeof refValue === 'undefined' ) {
      return '#6FBDD9'
    } else {
      return (sortDesc && value <= refValue || !sortDesc && value > refValue ) ? '#6FBDD9' : '#EDB61A'
    }
      
}
